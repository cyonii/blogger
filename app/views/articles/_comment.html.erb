<div>
  <h6>Comment by <%= comment.author_name %></h6>
  <small>Posted <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> later</small>

  <p class="comment">
    <%= comment.body %>
  </p>
</div>
